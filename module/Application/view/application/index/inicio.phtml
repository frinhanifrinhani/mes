
<div class="jumbotron">
    <div class="container">
        <h3 class="display-3">Bem vindo <?= $this->nome_participante ?>!</h3>
        <p>O My Easy Scrum é um gerenciador de projetos Scrum, que visa agilizar e facilitar 
            ainda mais os processos referentes ao gerenciamento e desenvolvimento de projetos, 
            e é muito bom ter você participando da equipe como <?= $this->tipo_participante ?>.</p>
        <?php
        switch ($this->cod_tipo_participante) {
            case 1:
                ?>
                <p><a class="btn btn-primary btn-lg" href="<?= $this->url('projeto') ?>" role="button">Ir para Projeto</a></p>
                <?php
                break;

            case 2:
                ?>
                <p><a class="btn btn-primary btn-lg" href="<?= $this->url('projeto') ?>" role="button">Ir para Projeto</a></p>
                <?php
                break;

            case 3:
                ?>
                <p><a class="btn btn-primary btn-lg" href="<?= $this->url('sprintbacklog-escolher-projeto') ?>" role="button">Ir para Sprint Backlog</a></p>
                <?php
                break;
        }
        ?>
    </div>
</div>
<div class="container">
    <!-- Example row of columns -->
    <div class="row">
        <div class="col-md-5">
            <h2>Projeto</h2>
            <p>Scrum é uma metodologia ágil para gestão e planejamento de projetos de software.
                No Scrum, os projetos são dividos em ciclos que vão de 15 a 30 dias chamados de Sprints.
                Esta conta do My Easy Scrum, possui 
            </p>
            <p>
                Esta conta do My Easy Scrum,
                <?php $totalProjeto = isset($this->projetos->totalProjeto) ? $this->projetos->totalProjeto : 0; ?>
                <?php if ($totalProjeto == 0) { ?>
                    ainda não possui Projeto cadastrado,
                <?php } ?>
                <?php if ($totalProjeto == 1) { ?>
                    possui <strong><?= $totalProjeto ?></strong> Projeto, 
                <?php } ?>
                <?php if ($totalProjeto > 1) { ?>
                    possui <strong><?= $totalProjeto ?></strong> Projetos, 
                <?php } ?>

                <?php $projetoFinalizado = isset($this->projetos->projetoFinalizado) ? $this->projetos->projetoFinalizado : 0; ?>
                <?php if ($projetoFinalizado == 0) { ?>
                    e ainda não possui Projeto concluido.
                <?php } ?>

                <?php if ($projetoFinalizado == 1) { ?>    
                    onde <strong><?= $projetoFinalizado ?></strong> Projeto já foi entrege.
                <?php } ?>

                <?php if ($projetoFinalizado > 1) { ?>    
                    onde <strong><?= $projetoFinalizado ?></strong> Projetos já foram entreges.
                <?php } ?>    
            </p> 
            <hr />
        </div>
        <div class="col-md-5">
            <h2>Sprint</h2>
            <p>Na metodoligia Scrum, o trabalho é realizado em iterações ou 
                ciclos que vão de 15 a 30 dias de calendário chamado de Sprints, o trabalho realizado 
                em cada sprint deve criar algo de valor tangível para o cliente ou usuário.
            </p>
            <p>
                Esta conta do My Easy Scrum,

                <?php $totalSprint = isset($this->sprints->totalSprint) ? $this->sprints->totalSprint : 0; ?>
                <?php if ($totalSprint == 0) { ?>
                    ainda não possui sprint cadastrada,
                <?php } ?>
                <?php if ($totalSprint == 1) { ?>
                    possui <strong><?= $totalSprint ?></strong> Sprint, 
                <?php } ?>
                <?php if ($totalSprint > 1) { ?>
                    possui <strong><?= $totalSprint ?></strong> Sprints, 
                <?php } ?>

                <?php $sprintFinalizado = isset($this->sprints->sprintFinalizado) ? $this->sprints->sprintFinalizado : 0; ?>
                <?php if ($sprintFinalizado == 0) { ?>
                    e ainda não possui sprint concluida.
                <?php } ?>

                <?php if ($sprintFinalizado == 1) { ?>    
                    onde <strong><?= $sprintFinalizado ?></strong> Sprint já foi entrege.
                <?php } ?>

                <?php if ($sprintFinalizado > 1) { ?>    
                    onde <strong><?= $sprintFinalizado ?></strong> Sprints já foram entreges.
                <?php } ?>    
            </p> 
            <hr />
        </div>
        <div class="col-md-5">
            <h2>Product Backlog</h2>
            <p>
                O Product Backlog é uma lista contendo todas as funcionalidades desejadas
                para um produto. O conteúdo desta lista é definido pelo Product Owner.
            </p>
            <p>
                Esta conta do My Easy Scrum,
                <?php $totalProductBacklog = isset($this->product_backlogs->totalProductBacklog) ? $this->product_backlogs->totalProductBacklog : 0; ?>
                <?php if ($totalProductBacklog == 0) { ?>
                    ainda não possui Product Backlog cadastrado,
                <?php } ?>
                <?php if ($totalProductBacklog == 1) { ?>
                    possui <strong><?= $totalProductBacklog ?></strong> Product Backlog, 
                <?php } ?>
                <?php if ($totalProductBacklog > 1) { ?>
                    possui <strong><?= $totalProductBacklog ?></strong> Product Backlogs, 
                <?php } ?>

                <?php $productBacklogFinalizado = isset($this->product_backlogs->productBacklogFinalizado) ? $this->product_backlogs->productBacklogFinalizado : 0; ?>
                <?php if ($productBacklogFinalizado == 0) { ?>
                    e ainda não possui Product Backlog concluido.
                <?php } ?>

                <?php if ($productBacklogFinalizado == 1) { ?>    
                    onde <strong><?= $productBacklogFinalizado ?></strong> Product Backlog já foi entrege.
                <?php } ?>

                <?php if ($productBacklogFinalizado > 1) { ?>    
                    onde <strong><?= $productBacklogFinalizado ?></strong> Product Backlogs já foram entreges.
                <?php } ?>    
            </p>
        </div>
        <div class="col-md-5">
            <h2>Sprint Backlog</h2>
            <p>
                O Sprint Backlog é uma lista de tarefas que o Scrum Team se
                compromete a fazer em um Sprint. Os itens do Sprint Backlog 
                são extraídos do Product Backlog, pela equipe, com base nas 
                prioridades definidas pelo Product Owner e a percepção da 
                equipe sobre o tempo que será necessário para completar as 
                várias funcionalidades.
            </p>
            <p>
                Esta conta do My Easy Scrum,
                <?php $totalSprintBacklog = isset($this->sprint_backlogs->totalSprintBacklog) ? $this->sprint_backlogs->totalSprintBacklog : 0; ?>
                <?php if ($totalSprintBacklog == 0) { ?>
                    ainda não possui Sprint Backlog cadastrado,
                <?php } ?>
                <?php if ($totalSprintBacklog == 1) { ?>
                    possui <strong><?= $totalSprintBacklog ?></strong> Sprint Backlog, 
                <?php } ?>
                <?php if ($totalSprintBacklog > 1) { ?>
                    possui <strong><?= $totalSprintBacklog ?></strong> Sprint Backlogs, 
                <?php } ?>

                <?php $sprintBacklogFinalizado = isset($this->sprint_backlogs->sprintBacklogFinalizado) ? $this->sprint_backlogs->sprintBacklogFinalizado : 0; ?>
                <?php if ($sprintBacklogFinalizado == 0) { ?>
                    e ainda não possui Product Backlog concluido.
                <?php } ?>

                <?php if ($sprintBacklogFinalizado == 1) { ?>    
                    onde <strong><?= $sprintBacklogFinalizado ?></strong> Sprint Backlog já foi entrege.
                <?php } ?>

                <?php if ($sprintBacklogFinalizado > 1) { ?>    
                    onde <strong><?= $sprintBacklogFinalizado ?></strong> Sprint Backlogs já foram entreges.
                <?php } ?>    
            </p>  
        </div>
    </div>
</div> <!-- /container -->